* `COUNT(*)` → counts **all rows** (includes NULLs)
* `COUNT(1)` → same as `COUNT(*)` (no difference in practice)
* `COUNT(column)` → counts **only non-NULL values** in that column

With `GROUP BY`, the same rules apply **per group**:

* `COUNT(*)` → total rows in each group
* `COUNT(column)` → non-NULL values in each group
 
